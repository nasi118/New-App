function TagManagerModal({ tags, setTags, onClose, onTagUpdate, onTagDelete }) {
  // tags: array of strings
  const [editing, setEditing] = useState(null);
  const [newTag, setNewTag] = useState("");
  // etc
}
